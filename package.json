{
  "name": "fast-date-formatter",
  "version": "1.0.0",
  "description": "fast date formatter",
  "main": "DateFormatter.js",
  "scripts": {
    "unit": "tap test/*.test.js",
    "test": "npm run lint && npm run unit",
    "lint": "./node_modules/eslint/bin/eslint.js index.js lib/* test/*",
    "coveralls": "npm run unit --  --cov",
    "coverage": "npm run coveralls && tap --coverage-report=lcov",
    "benchmark": "node benchmark/benchmark.js"
  },
  "author": "Denis Fäcke",
  "license": "MIT",
  "devDependencies": {
    "benchmark": "^2.1.4",
    "coveralls": "^3.0.1",
    "date-format": "^1.2.0",
    "dateformat": "^3.0.3",
    "eslint": "^4.19.1",
    "moment": "^2.22.2",
    "tap": "^12.0.1"
  }
}
